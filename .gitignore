# Gradle / build (unanchored so every module's build dir is covered)
.gradle/
build/
.kotlin/
bin/

# Tooling / planning artifacts (not project source)
/.artifacts/

# Android
/local.properties
*.apk
*.aab
.externalNativeBuild
.cxx
/captures

# IDE / editor
*.iml
/.idea/
/.vscode/
.DS_Store

# Firebase config — provide per developer, do not commit.
/app/google-services.json

# Secrets — NEVER commit (public repo)
**/*firebase-adminsdk*.json
/backend/secrets/
*.env
